# The Gambonanza build this checkout of the framework was last verified against.
#
# The framework binds to a lot of private game state by name (reflection), so a
# game update is the usual suspect when something stops working. build.sh hashes
# the vanilla Assembly-CSharp.dll on every run and prints a note when it differs
# from what's recorded here.
#
# To re-verify after a game update:
#   1. ./build.sh
#   2. Launch the game, open the console (F10), run `mods` and `list gambits`.
#   3. If everything works, update the two values below and note what changed.

steamBuildId=25059529
assemblySha256=15610e2a591e5183935919660c430388699c8cc6072cbb8b8c6800c77af4b5e0
unityVersion=6000.4.3f1
verifiedOn=2026-09-04

# Build 25059529 (verified 2026-09-04): clean per-type ILSpy diff against
# 24858528, both decompiled with the same reference set (a first pass without
# one produced 676 files of op_Implicit noise - always pass -r <Managed>).
# 8 of 808 decompiled types changed. All 56 by-name bindings in src/ and
# sample_mods/ (patcher targets included) were mechanically confirmed to still
# exist in the new assembly. Live boot on the new build with 3 mods loaded
# (CrumbleApi hooked CrumbleManager.PlayerTurnEffects, a Freeze-holding test
# gambit worked in a real run), zero exceptions; the Mod Manager re-patched
# the new dll with the unchanged patcher. Unity version unchanged.
# What the update actually is:
#   - 16:9 letterbox: RatioStarter.ApplyLetterbox() sets Camera.main.rect to
#     a letterbox/pillarbox for odd window sizes (skipped on Steam Deck),
#     called from GameManager.Start, SettingsCanvas resolution/fullscreen
#     changes and on window resize. Framework UI (console, injected buttons,
#     GameUI modals) is ScreenSpaceOverlay uGUI, unaffected by the camera
#     rect; KamikazeGambit raycasts from the game's own
#     SelectionManager.PointerPosition, so the conversion is vanilla's.
#   - The graveyard survives a stalemate retry: BaseData.Pat_Graveyard,
#     copied in SaveManager/PatManager. Nothing in the framework or bundled
#     mods reads BaseData by layout; KamikazeGambit's AddPieceToGraveyard
#     reflection target is unchanged.
#   - Gambit_Barter now also fires on SHOP-after-GRAVEYARD, GambitKeyOfLife
#     also on BOARD_PLACEMENT: vanilla gambit logic, GambitApi untouched.
#   - Injection targets: GameManager.Start gained one ApplyLetterbox() call
#     (LoadAll is still prepended ahead of it); SettingsCanvas.OnEnable,
#     CanvasMenu.OnEnable and AchievementManager are byte-identical.
#   - CrumbleManager, TurnManager, CrumbleIndicator, LoadManager and
#     TileBehaviour are identical (CrumbleApi territory).
#   - Not audited: the 16 other files the update touched (assets and a few
#     Managed/System DLLs). Nothing of ours binds to them.
#
# Build 24858528 (verified 2026-08-21): per-type diff against 24648699 done
# (40 of ~700 types changed), then a full multi-agent audit with adversarial
# verification of every claim. Verified by full ./build.sh, a live boot (all 6
# bundled mods loaded, achievement gate fired on the startup tutorial check,
# zero exceptions), and a line-by-line classification of all 40 diffs.
# What the update actually is:
#   - 19 types are pure churn (Steamworks.NET ref->out params, UnityWebRequest
#     .Result typed compares). The achievement gate targets method NAMES on
#     AchievementManager, whose two guarded methods kept their exact signatures.
#   - Steam Deck UI pass: ControlSchemeManager.IsSteamDeck detection + scaling
#     sprinkled through UI code; the joystick path now hides+locks the hardware
#     cursor earlier (only affects controller play). Boss-info screen rework.
#   - Gambit sorting-order/highlight refactor (GambitBehaviour/GambitInfo/
#     GambitInfoCollection/GambitToBuy incl. gachapon highlight bookkeeping) -
#     GambitApi territory; compiles and loads clean, screens still deserve an
#     in-game look (follow-up port-review task).
#   - New enemy-ability warning feedback (PieceEnemyModifier/PieceVisualEffect +
#     SelectionManager/InterfaceManager wiring). KNOWN VANILLA 1.5.0 BUG: it
#     leaks infinite Yoyo DOFade tweens + an alarm-shake Sequence on captured
#     Magician/Crumbler/Invoker pieces (unsubscribes only in OnDestroy, never
#     DOKills) -> repeating safe-mode-captured SpriteRenderer DOFade errors in
#     Player.log after captures. Harmless noise, not framework-caused.
#   - Injection targets GameManager.Start, CanvasMenu.OnEnable byte-identical;
#     SettingsCanvas.OnEnable diff is Steam Deck churn only. No changed type
#     touches Time.timeScale, physics, or Rigidbody anywhere.
#   - Pachinko/gachapon flow (CanvasPachinko/PachinkoBehaviour/PachinkoBall/
#     compartments) is byte-identical between builds. A ball stuck on the top
#     pin is reachable in PURE VANILLA: the token releases kinematic->dynamic
#     with exactly zero velocity from a player-timed x, so a dead-center launch
#     can settle balanced on the top pin and the Rigidbody2D sleeps. No
#     watchdog exists; designed recovery = the left/right HIT buttons that
#     appear 5s after launch (AddForce impulse wakes a sleeping body), or
#     quit-to-menu + continue (state PACHINKO is saved at purchase; the ball
#     and reward tiles respawn intact). Audited: neither the framework, any
#     mod, nor the achievement gate can touch the ball (zero physics/timeScale/
#     EventSystem surface in all mod+framework code; gate call sites are all
#     fire-and-forget voids; no Steam callbacks awaited anywhere in the game).
#   - Only Steam readback in gameplay: Scheme_Sky polls GetAchievement
#     ("07_ACH_LAND") to unlock the cosmetic Sky board scheme. With the v1.4.0
#     gate pausing stat_land, Sky cannot NEWLY unlock during modded play
#     (documented in CHANGELOG 1.4.0); already-earned unlocks are unaffected.
#
# Build 24648699 (verified 2026-08-15): no per-type diff was possible - the
# previous build's Assembly-CSharp.dll was already gone from refs/ by the time
# the update was noticed, so there is nothing to diff against. Verified the
# empirical way instead: full ./build.sh, launched, all 7 mods loaded via
# ModHost, console and GambitApi working, collection and run-info screens
# inspected. If something does break, this un-diffed update is the first
# suspect. To diff a future update, copy refs/Assembly-CSharp.dll aside BEFORE
# re-running build.sh - it overwrites refs/ from the installed game.
#
# What the PREVIOUS build (24613134, game v1.4.0) changed that the framework
# cares about:
#   - New Graveyard buy-back system: State.GRAVEYARD (appended at the enum END,
#     so compiled mods keep working), GraveyardManager/Canvas/Button/Infos,
#     PieceManager.AddPieceToGraveyard, BaseData.Graveyard/GraveyardPieceBought.
#     The state machine can now pass WIN -> GRAVEYARD -> SHOP after boss levels.
#     KamikazeGambit feeds sacrificed pieces into the pool (via reflection, so
#     its prebuilt DLL still loads on pre-graveyard builds).
#   - WarHorse gambit replaced IN PLACE by Clown: Unlock_Infos.WAR_HORSE ->
#     CLOWN at the same ordinal (no enum shift), BaseData.WarHorse_Unlock ->
#     Clown_Unlock, types Gambit_WarHorse/WarHorse_GambitUnlock removed,
#     Gambit_Clown/Clown_GambitUnlock added. Vanilla gambit count is still 200.
#   - GambitLibrary.SelectGambits now dedups on SO_Gambit.GambitName instead of
#     UnityEngine.Object.name (GambitApi already assigns both uniquely).
#   - SettingsCanvas gained a sixth "Accessibility" tab (m_Accessibility /
#     m_AccessibilityContainer) plus ~5 new gameplay-tab rows (Skip Animations,
#     Ask Before Open Graveyard, Cursor, Shake Screen, Tile Coordinates).
#     Tab BUTTONS are discovered by field TYPE (ButtonSettings) since framework
#     v1.1.0 - the old name-suffix match missed m_Customize/m_Accessibility and
#     they leaked into cloned mod modals.
#   - New types the framework doesn't bind: BoardStructureManager, SkipBossUI,
#     AchievementType, RunInfo_AchievementHint, AccessibiltySettings (sic),
#     WarningSettingsBehaviour, GraveyardButton.
#   - Audited ~112 reflection/compile-time bindings across 36 game types:
#     everything else the framework touches (GameManager.Start,
#     CanvasMenu.OnEnable, SettingsCanvas.OnEnable, GambitLibrary buckets,
#     GambitCollectionSlide, RunInfoCanvas, StockManager, ChessDataManager,
#     TileBehaviour, BossManager, LocalizationManager, scheme/customize types)
#     is unchanged. Assets grew to 925 sprites / 236 textures (gallery: 561/236).
